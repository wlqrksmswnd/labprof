# 실습실 수동 검증 체크리스트 (2인 왕복)

여러 사람이 같은 PC를 쓰는 상황을 실제로 재현해서 확인한다.
**여기 있는 건 실제 PC·실제 Chrome·실제 Google 계정이 없으면 확인할 수 없는 것들이다.**

- 소요: 30~40분 (로그인 2회 포함)
- 필요: 사람 2명 (또는 Google 계정 2개), 폰(2차인증용)
- 한 명을 **A**, 다른 한 명을 **B**라고 부른다

> 실패한 항목은 아래 "기록"에 적어 두고 진행을 멈춘다. 특히 5번과 6번은 남의 프로필을 지키는 장치이므로, 통과하지 못하면 여러 사람이 같이 쓰지 않는 것이 맞다.
> 오류가 나면 → **[오류가 났을 때](#오류가-났을-때)** (맨 아래). **다시 실행하기 전에** 그 절을 먼저 볼 것.

---

## 0단계 — 시작 전 확인

- [ ] 폴더 전체가 `D:`에 있다 (예: `D:\GoogleLab\`) — 바탕화면이나 `C:`가 아니다
- [ ] **`unblock.bat`을 한 번 실행했다** (D:에 복사한 직후 1회). 인터넷에서 받은 파일의 차단
      표시를 지운다. `unblock.bat` **자신에서만** 보안 경고가 한 번 뜰 수 있다 → "실행" 을 누르고
      "이 파일을 열기 전에 항상 확인" 체크가 있으면 해제한다
- [ ] `env-log.txt`의 두 블록에서 `SID`와 `UserDpapiKeys`가 **같다**
      (아직 안 했으면 `README.md`의 0단계를 먼저. 이게 다르면 아래 전부 무의미하다)
- [ ] 그 두 블록 사이에 **"다시 시작"이 아니라 완전 종료 → 켜기**를 했다.
      실습실 초기화는 종료할 때 일어나므로, 재시작으로 같게 나온 것은 근거가 약하다
- [ ] `env-log.txt`의 `BlockedFiles` 줄이 `(없음)` 이다 (`unblock.bat` 이 실제로 먹었다는 뜻)
- [ ] 폴더에 `profile-*.enc`가 하나도 없다 (깨끗한 상태에서 시작)
- [ ] Chrome 창이 하나도 열려 있지 않다

### 0-1 — 첫 실행에서 보안 경고 창이 또 뜬다면

PowerShell 창이 뜨기도 **전에** 오류·경고 창이 뜨는 경우다. 그 시점에는 이 도구의 코드가 한 줄도
실행되지 않았으므로 스크립트 버그가 아니고, `run-log.txt`에도 남지 않는다. 원인이 둘이라 가려야 한다.

- [ ] 창의 **문구를 그대로 적어 둔다** (사진이 가장 확실하다). 이게 유일한 단서다
- [ ] `collect-env.bat` 을 돌려 `env-log.txt` 의 `BlockedFiles` 줄을 본다
- [ ] `BlockedFiles` 에 파일 이름이 **있다** → 차단 표시(MOTW)다. `unblock.bat` 을 다시 실행한다
      (D: 가 읽기 전용이면 실패한다 — 그때는 파일 우클릭 → 속성 → 맨 아래 "차단 해제")
- [ ] `BlockedFiles` 가 `(없음)` 인데도 뜬다 → **백신이나 실습실 정책**이다. 코드로 우회할 수
      없으므로 관리자에게 이 폴더의 예외 등록을 요청한다. 적어 둔 문구를 함께 가져간다

---

## 1 — A가 자기 프로필을 만든다

- [ ] `start.bat` 실행
- [ ] 프로필이 없으므로 메뉴 없이 바로 이름을 묻는다 → `a` 입력
- [ ] 컨테이너 비밀번호를 두 번 입력 (**영어 대소문자와 숫자만.** **A의 것. B에게 알려주지 않는다.** Google 비밀번호와 다르게)
- [ ] 빈 Chrome이 열린다
- [ ] **첫 실행 안내 화면과 "Chrome을 기본 브라우저로 설정" 대화상자가 뜨지 않는다**
      (뜨면 실행 플래그가 안 먹은 것이다. 기본 브라우저로 지정해 버리면 임시 폴더의 프로필이
       기본이 되므로 여기서 멈추고 기록한다)
- [ ] A의 Google 계정으로 로그인 (이메일 + 비밀번호 + 폰 2차인증)
- [ ] **2차인증 화면에서 "이 기기에서 다시 묻지 않음" 체크** ← 놓치면 이 검증의 의미가 없다
- [ ] `chrome://version`의 프로필 경로가 `...\Temp\lp`인지 확인
- [ ] Chrome을 X로 정상 종료
- [ ] 콘솔에 `저장 완료: profile-a.enc (NNMB)` 가 뜬다
- [ ] 폴더에 `profile-a.enc`가 생겼다
- [ ] `%LOCALAPPDATA%\Temp\lp` 폴더가 **사라졌다** (평문 프로필이 남지 않았다)

실패하면: `저장 완료`가 안 뜨고 오류가 나면 그 메시지를 기록. 컨테이너가 안 만들어졌을 뿐 잃은 것은 없다.

---

## 2 — B가 자기 프로필을 만든다 (A 것과 나란히)

- [ ] `start.bat` 실행
- [ ] 이번엔 **메뉴가 뜬다** (`1) a`, `N) 새 프로필 만들기`)
- [ ] `N` → 이름 `b` 입력
- [ ] B의 비밀번호를 두 번 입력 (**영숫자만. A와 다른 비밀번호**)
- [ ] B의 Google 계정으로 로그인 + "다시 묻지 않음" 체크 → 정상 종료
- [ ] `저장 완료: profile-b.enc` 확인
- [ ] 폴더에 `profile-a.enc`와 `profile-b.enc`가 **둘 다** 있다
- [ ] `profile-a.enc`의 수정 시각이 1단계 때 그대로다 (B의 작업이 A 파일을 건드리지 않았다)

---

## 3 — A가 다시 들어간다 (핵심: 로그인이 사라졌는가)

- [ ] `start.bat` 실행
- [ ] 메뉴에 `a`와 `b`가 둘 다 보인다
- [ ] 최근에 저장한 `b`가 1번, `a`가 2번으로 정렬돼 있다
- [ ] `a` 선택 (번호 `2` 또는 이름 `a` 둘 다 시도해 본다)
- [ ] A의 비밀번호 입력 → `컨테이너 열림 (마지막 저장: ...)` 표시
- [ ] **Chrome이 A 계정으로 이미 로그인된 상태로 열린다 — 이메일·비밀번호·2차인증·폰 전부 없이**
- [ ] Gmail 등을 열어 실제로 A 계정인지 확인
- [ ] 정상 종료 → `저장 완료`

여기서 로그인 화면이 뜨면: 컨테이너는 열린 것이므로(`컨테이너 열림` 줄이 나왔으므로) Google 쪽 세션 만료다.
`README.md`의 "두 가지 실패를 구분하는 법" 참고. 1단계의 "다시 묻지 않음" 체크를 놓쳤을 가능성이 가장 크다.

### 3-1 — 이름을 잘못 쳐도 빈 프로필이 만들어지지 않는다

`start.bat a` 처럼 이름을 인자로 주는 방식은 메뉴를 건너뛴다. 오타가 그대로 통과하면
빈 프로필이 새로 만들어져 사용자는 자기 것을 잃은 줄 안다.

- [ ] `start.bat aa` 를 실행한다 (있는 이름 `a` 의 오타를 흉내낸 것)
- [ ] `'aa' 이라는 프로필은 없습니다.` 와 함께 **폴더에 있는 이름들(`b`, `a`)이 같이 보인다**
- [ ] 그냥 Enter를 눌러 **취소된다** — `y`/`yes` 외에는 전부 거부다 (기본값이 거부)
- [ ] `profile-aa.enc` 가 만들어지지 않았고, Chrome도 뜨지 않았다
- [ ] `start.bat a` (정확한 이름)는 메뉴 없이 바로 A의 비밀번호를 묻는다

---

## 4 — 남의 프로필은 열리지 않는다

- [ ] `start.bat` 실행 → **`b`** 선택 → **A의** 비밀번호 입력
- [ ] `'b' 의 비밀번호가 틀렸거나 파일이 손상되었습니다.` 가 뜬다
- [ ] **Chrome이 아예 뜨지 않는다**
- [ ] `profile-b.enc`의 수정 시각과 크기가 변하지 않았다 (실패가 남의 파일을 건드리지 않았다)
- [ ] `%LOCALAPPDATA%\Temp\lp` 가 없다 (실패 시 평문이 남지 않았다)

이게 이 도구의 보안 경계다. 통과하지 못하면 여기서 멈춘다.

---

## 5 — 진행 중인 세션 위로 시작할 수 없다

- [ ] A가 `start.bat`으로 들어가 **Chrome을 열어 둔 채로** 둔다 (자리를 비운 상황)
- [ ] 그 상태에서 B가 `start.bat`을 **또** 실행한다
- [ ] `이 PC 에서 이미 프로필 세션이 실행 중입니다.` 가 뜬다
- [ ] **비밀번호를 묻지 않고** 바로 종료된다 (슬롯 메뉴도 뜨지 않는다)
- [ ] A의 Chrome이 그대로 살아 있다 (창이 닫히거나 프로필이 초기화되지 않았다)
- [ ] A가 Chrome을 정상 종료 → `저장 완료` 확인
- [ ] 그 후 B가 `start.bat` 실행 → 이번엔 정상적으로 메뉴가 뜬다

이 검사가 없으면 B의 복호화가 A의 열린 프로필을 덮어써서 A의 그 세션이 깨진다.

---

## 6 — 남은 평문 폴더가 다음 사람 앞에 놓이지 않는다

비정상 종료를 일부러 만든다.

- [ ] A가 `start.bat`으로 들어가 Chrome이 열린 상태로 만든다
- [ ] **스크립트 콘솔 창을 X로 강제 종료**한다 (저장 단계를 건너뛰게)
- [ ] 작업 관리자에서 `chrome.exe`를 모두 **강제 종료**한다
- [ ] `%LOCALAPPDATA%\Temp\lp` 폴더가 **남아 있는 것**을 확인한다 (A의 평문 프로필이다)
- [ ] B가 `start.bat`을 실행한다
- [ ] `이전 세션이 남긴 임시 폴더를 정리합니다.` 가 **비밀번호 프롬프트보다 먼저** 뜬다
- [ ] 그 시점에 `%LOCALAPPDATA%\Temp\lp` 가 **사라졌다**
- [ ] 여기서 B가 일부러 **틀린 비밀번호**를 넣고 종료해도, `Temp\lp` 에 A의 프로필이 없다

정리에 실패하면 `임시 폴더를 지우지 못했습니다` 와 함께 Chrome을 띄우지 않고 종료돼야 한다.
(남의 프로필 위에서 시작하는 것보다 안전하다. 재부팅하면 `C:`가 초기화되며 함께 사라진다.)

- [ ] A가 `start.bat` → `a` 로 다시 들어가, **로그인이 유지되어 있다** (강제 종료로 잃은 것은 그 세션의 변경뿐)

### 6-1 — 평문 zip도 남지 않는다

압축·해제 중간에는 프로필 전체가 `%TEMP%`에 평문 zip(`lp-<32자>.zip`)으로 한 번 놓인다.
정상 경로에서는 바로 지워지지만, 하필 그 몇 초 사이에 창이 닫히면 남는다.
폴더만 치우고 이걸 놔두면 정리한 것이 아니다.

- [ ] 여기까지 온 시점에 `%TEMP%` 에 `lp-*.zip` 이 **하나도 없다**
      (탐색기 주소창에 `%TEMP%` 를 붙여넣고 `lp-` 로 검색)
- [ ] 남아 있다면 → `start.bat` 을 한 번 실행한다. 비밀번호를 묻기 전에
      `이전 세션이 남긴 임시 파일 N개를 정리했습니다.` 가 뜨고 사라진다

---

## 7 — 비밀번호 변경이 고른 슬롯만 바꾼다

- [ ] `change-password.bat` 실행
- [ ] 메뉴에서 `a` 선택
- [ ] 기존 비밀번호 1회 + 새 비밀번호 2회
- [ ] `비밀번호를 변경했습니다.` 표시
- [ ] `start.bat` → `a` → **새** 비밀번호로 열린다
- [ ] `start.bat` → `a` → **옛** 비밀번호로는 열리지 않는다
- [ ] `start.bat` → `b` → B의 비밀번호로 **그대로 열린다** (영향 없음)
- [ ] 옛 비밀번호가 노출돼서 바꾼 경우라면 `profile-a.enc.bak` 을 삭제했다
      (`.bak`은 아직 옛 비밀번호로 열린다)

---

## 8 — 비밀번호 문자 제한 (영숫자만)

제한은 **새로 정하는 자리에만** 걸린다. 여는 자리에 걸리면 기존 컨테이너가 잠기므로,
"거절된다" 와 "그래도 열린다" 를 둘 다 확인해야 의미가 있다.

- [ ] `start.bat` → `N` → 이름 `z` → **한/영 키를 켜고** 한글 비밀번호 입력
- [ ] `영어 대소문자와 숫자만 쓸 수 있습니다` + `한글 입력기(한/영)가 켜져 있지 않은지 확인하세요.` 가 뜨고 종료된다
- [ ] 확인 입력(`새 비밀번호 확인`)을 **묻지 않고** 바로 거절한다
- [ ] `profile-z.enc` 가 만들어지지 **않았다**
- [ ] 다시 `start.bat` → `N` → `z` → `abc123` 은 통과하고 Chrome 이 열린다 → 정상 종료
- [ ] `change-password.bat` → `z` → 기존 `abc123` + 새 비밀번호에 **공백이나 `!`** 를 넣는다
      → 거절되고 `profile-z.enc.tmp` 가 남지 않고 `profile-z.enc` 의 수정 시각도 그대로다
- [ ] 확인 끝나면 `profile-z.enc*` 를 지운다

**A/B 의 기존 컨테이너는 이 항목 때문에 다시 만들지 않는다.** 옛 비밀번호로 계속 열려야
정상이고, 열리지 않으면 그게 이번 변경의 실패다.

---

## 오류가 났을 때

실습실에서 그 자리에서 고칠 때 필요한 것들.

### 먼저 — 다시 실행하지 말 것

`start.bat`은 비밀번호를 묻기 전에 `%LOCALAPPDATA%\Temp\lp`를 지운다. **Chrome을 닫은 뒤 저장 단계에서 터진 경우라면 그 폴더가 그 세션의 유일한 사본이다.** 재실행하면 그 세션의 로그인·설정 변경이 사라진다.

- [ ] `%LOCALAPPDATA%\Temp\lp` 가 남아 있는지 본다 (탐색기 주소창에 그대로 붙여넣기)
- [ ] 남아 있고 그 세션이 아깝다면 → 폴더째로 다른 곳에 복사해 두고 나서 재실행한다
- [ ] `run-log.txt` 를 먼저 확인한다 (아래)

저장 단계에서 터진 경우에는 스크립트가 그 폴더 경로를 화면에 같이 찍어 준다
(`작업 폴더: C:\Users\...\Temp\lp`). 그 줄이 보이면 폴더가 아직 있다는 뜻이다.

**기다리는 중에 `Ctrl+C` 로 멈추는 것은 안전하다.** 컨테이너는 손대지 않은 상태이고
(저장은 시작조차 하지 않았다), 작업 폴더는 그대로 남는다. 그 세션의 변경분이 아깝다면
위와 같이 폴더를 복사해 두고 나서 재실행한다. Chrome이 아직 열려 있으면 먼저 닫는다 —
열어 둔 채 재실행하면 5번 검사에 걸려 시작되지 않는다.

**컨테이너 자체는 어떤 오류에서도 안전하다.** 새 파일이 MAC 검증을 통과하기 전에는 기존 파일을 건드리지 않는다. 최악의 비용은 로그인 1회다.

### 어디를 보는가

| 볼 것 | 무엇이 있나 |
|---|---|
| 화면 | `start.bat` 은 끝에 `pause` 가 있어 창이 닫히지 않는다. 마지막 몇 줄이 핵심 |
| `run-log.txt` | 화면과 같은 내용이 `start.bat` 옆(`D:`)에 쌓인다. **PC를 껐다 켜도 남는다** |
| `boot-error.txt` | **평소에는 생기지 않는다.** PowerShell 이 시작조차 못 했거나 로그를 열기 전에 죽은 경우만 남는다. 있다는 것 자체가 단서다 (콘솔에도 `*** see boot-error.txt ***` 가 찍힌다) |
| 스택 트레이스 | 오류 밑에 회색으로 `파일:줄` 이 찍힌다. 어느 스크립트 몇 번째 줄인지 |

`run-log.txt` 에서 그 실행만 보려면 `[start] 2026-09-08 21:31:13` 같은 머리말을 찾아 그 아래를 읽는다. 실행마다 하나씩 붙는다.

- [ ] `run-log.txt` 에서 해당 실행 블록을 찾았다
- [ ] 몇 단계에서, 어떤 메시지로 멈췄는지 아래 "기록"에 적었다

로그 파일이 안 만들어졌다면 (`기록 파일을 열지 못했습니다` 가 떴다면) `D:` 가 읽기 전용이거나 다른 사람 세션이 파일을 잡고 있는 것이다. 도구는 그대로 동작하므로 화면을 사진으로 남기면 된다.

### 흔한 것

| 화면 | 뜻 / 할 일 |
|---|---|
| `컨테이너 열림` 이 나온 뒤 로그인 화면 | 비밀번호는 맞았다. Google 세션 만료다 → 다시 로그인 + "다시 묻지 않음" 체크 후 정상 종료 |
| `비밀번호가 틀렸거나 파일이 손상되었습니다` | Chrome이 아예 안 뜬다. 남의 슬롯을 골랐거나 오타. **파일은 그대로다** |
| `Chrome 프로세스를 확인할 수 없습니다` | 실행 실패. **저장을 건너뛰므로 기존 컨테이너는 안전하다.** 다시 실행 |
| `임시 폴더를 지우지 못했습니다` | 남은 `chrome.exe` 가 폴더를 잡고 있다. 작업 관리자에서 다 죽이거나 재부팅 |
| `이미 프로필 세션이 실행 중입니다` | 5번 검사가 걸린 것 — 정상 동작이다. 열린 Chrome 을 모두 닫는다 |
| 저장된 줄 알았는데 지난번 상태로 열린다 | **Chrome 을 열어둔 채 PC 를 껐을 가능성이 가장 크다.** 저장은 Chrome 이 닫히는 것을 신호로 시작하므로 그 세션은 저장되지 않는다 (`run-log.txt` 의 그 블록에 `--- 저장 중 ---` 이 없으면 확정이다). 로그인은 유지되고, 잃은 것은 그날의 변경분뿐이다 |
| 창을 다 닫았는데 저장이 시작되지 않는다 | 확장 프로그램이 백그라운드로 살아 있는 것. 작업 관리자에서 `chrome.exe` 를 모두 죽이면 저장이 이어진다. (`--disable-background-mode` 로 막아 두었으므로 이게 보이면 기록할 것) |
| 그 외 `오류:` + 스택 | 스택의 `파일:줄` 을 보고 고친다 |

### 스크립트를 그 자리에서 고칠 때

- [ ] **인코딩을 깨뜨리지 않는다.** `.ps1`은 **UTF-8 with BOM + CRLF** 여야 한다. BOM이 없으면 PowerShell 5.1이 CP949로 읽어서 **한글 메시지가 전부 깨진다.**
      메모장은 기존 파일을 열어 `Ctrl+S`로 저장하면 원래 인코딩을 유지하지만, "다른 이름으로 저장"이나 새 파일은 기본이 **BOM 없는 UTF-8** 이다. 그때는 인코딩 드롭다운에서 `UTF-8(BOM)`을 고른다.
- [ ] 저장한 뒤 BOM이 살아 있는지 확인한다 (`True` 여야 한다):

```powershell
$b = [IO.File]::ReadAllBytes('.\lab-profile.ps1')
$b[0] -eq 0xEF -and $b[1] -eq 0xBB -and $b[2] -eq 0xBF
```

- [ ] 고친 뒤 실행 전에 문법만 훑어본다:

```powershell
Get-ChildItem *.ps1 | ForEach-Object { $t=$null; $e=$null;
  [void][System.Management.Automation.Language.Parser]::ParseFile($_.FullName,[ref]$t,[ref]$e)
  if ($e.Count) { "$($_.Name): $($e[0].Message)" } else { "ok $($_.Name)" } }
```

- [ ] 라이브러리 함수만 따로 불러 볼 수도 있다 (로드 시 부작용이 없다):

```powershell
. .\crypto.ps1; . .\profile-lib.ps1; . .\slots.ps1
@(Get-LPSlots -Root .)        # 슬롯이 잡히는지
Get-LPChromeCount -Marker (Join-Path $env:LOCALAPPDATA 'Temp\lp')
```

- [ ] 고치기 전 원본을 복사해 둔다 (`copy lab-profile.ps1 lab-profile.ps1.bak`). 이 폴더는 git 이 아니라 되돌릴 수단이 없다.

### 그래도 안 되면

이 도구를 포기해도 로그인 자체는 된다. `README.md` 의 "안 될 때 — 폴백" (KeePassXC + TOTP, FIDO2 보안키). 0단계에서 `SID`/`UserDpapiKeys` 가 달랐다면 이 방식은 이 PC 에서 원리적으로 안 되는 것이므로, 고치려 하지 말고 폴백으로 간다.

---

## 마무리

- [ ] 검증용으로 만든 슬롯을 정리한다 — `profile-a.enc` / `profile-b.enc` 와 각 `.bak` 삭제
- [ ] 검증에 쓴 계정을 Google 계정 → 보안 → 내 기기 에서 **원격 로그아웃**
      (검증용으로 심은 기기 신뢰 쿠키를 남기지 않는다)
- [ ] 실제로 쓸 이름으로 1단계를 다시 한다

---

## 기록

| 항목 | 결과 | 메모 |
|---|---|---|
| 0 시작 전 확인 | | |
| 1 A 프로필 생성 | | |
| 2 B 프로필 생성 | | |
| 3 A 재진입 (로그인 없음) | | |
| 4 남의 프로필 거부 | | |
| 5 진행 중 세션 차단 | | |
| 6 잔여 폴더 정리 | | |
| 7 비밀번호 변경 격리 | | |
| 8 비밀번호 문자 제한 | | |

- 실습실 / 좌석 번호:
- 검증 날짜:
- Chrome 버전 (`chrome://version`):

### 적어 올 것 — `run-log.txt` 의 두 줄

저장이 끝날 때마다 이 두 줄이 로그에 남는다. **다음 속도 개선의 유일한 근거이므로 그대로 옮겨 온다.**

```
[시간] 키유도 __s  열기 __s  캐시 __s  저장 __s  정리 __s
남은 큰 폴더:
```

- [ ] 백신 실시간 검사에서 `%LOCALAPPDATA%\Temp\lp` 를 예외로 등록해 줄 수 있는지 관리자에게 확인한다.
      가능하면 **예외 전/후의 `[시간]` 줄을 둘 다** 적어 온다 (여기서 가장 큰 차이가 날 것으로 본다).

> 다른 좌석에서도 쓸 생각이라면, 그 좌석에서 `collect-env.bat`을 한 번 돌려 `env-log.txt`의 `SID`·`UserDpapiKeys`를 이 좌석 것과 비교해 본다.
> 같으면 컨테이너를 좌석 간에 옮겨도 열릴 가능성이 있고, 다르면 좌석마다 따로 셋업해야 한다.

**이 순서를 건너뛴 실제 사례 (PC18-18, 2026-09-10).** 2차 검증은 4분 안에 네 번 실행하는 것으로 끝났다 — 재부팅이 없었고 `collect-env.bat` 도 돌리지 않아 `env-log.txt` 가 없다. 그래서 **그 좌석에서 이 방식이 되는지는 아직 측정되지 않았다.** 같은 세션 안에서 컨테이너가 열리는 것은 DPAPI 키가 유지된다는 증거가 아니다 (한 번도 안 바뀌었으므로). 좌석을 바꾸면 반드시 `collect-env.bat` → **완전 종료** → 켜기 → `collect-env.bat` → `start.bat` 순서를 다시 밟는다.
